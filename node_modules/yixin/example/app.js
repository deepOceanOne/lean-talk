/**
 * Created by fuzesun on 14-5-14.
 */
var express = require('express');
var app = express();
var Yixin = require('./');
var yixin = new Yixin('token');

app.use(express.query());//this should before
app.use('/yinxin', yixin.middleware());
app.use(express.bodyParesr());//this should behind `yixin.middleware`

app.configure(function() {
  app.use(express.query());
  yixin.text(function(req, res) {
    var message = req.yixin;
    res.reply('hello world');
  }).image(function() {
    //...
  });
});

